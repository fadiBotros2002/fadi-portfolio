# Civil Registry System

| | |
|--|--|
| Catalog id | `civil-registry` |
| Role on site | .NET / SQL Server sample |
| Period | August 2023 |
| Stack (tags) | ASP.NET Web Forms, C#, SQL Server |

## What it is

Secure CRUD for civil records and authentication on ASP.NET Web Forms + SQL Server.

## Portfolio wiring

- Icon-only card (no `image` / gallery currently).
- GitHub: profile link.

## Edit tips

Add screenshots later via `image` + `gallery` like other cards; update i18n `projects.items.civil-registry` in all locales.
