# Static portfolio — publish from repo root
[build]
  publish = "."

# CV links: /p/odoo-food-wms → /?project=odoo-food-wms (root URL so CSS/JS resolve)
[[redirects]]
  from = "/p/:project"
  to = "/?project=:project"
  status = 302
